on:
  push

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: ubuntu:16.04

    steps:
    - uses: actions/checkout@v1
    - name: 运行多行脚本
      run: |
        apt install sudo python git
